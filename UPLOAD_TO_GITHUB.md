# Upload checklist

Upload the CONTENTS of this folder to the repository root. Do not upload only the ZIP.

You should see `index.html`, `app.js`, `data.js`, and `styles.css` directly on the GitHub repository home page. Keep `assets/` and `content/` as folders.

Then enable GitHub Pages from `Settings → Pages → Deploy from a branch → main → /(root)`.
